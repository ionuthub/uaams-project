// app/register/layout.js
// Per-route page title (WCAG 2.2 SC 2.4.2 Page Titled, issue #148).

export const metadata = {
  title: "Create an account - UAAMS",
  description: "Register as an applicant to apply to universities through UAAMS.",
};

export default function RegisterLayout({ children }) {
  return children;
}
